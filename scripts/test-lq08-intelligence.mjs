import assert from"node:assert/strict";import{readFile}from"node:fs/promises";const read=p=>readFile(new URL(`../${p}`,import.meta.url),"utf8");const[client,ui,css,page,nav]=await Promise.all([read("lib/jobs/intelligenceClient.ts"),read("components/jobs/IntelligenceWorkspace.tsx"),read("components/jobs/Intelligence.module.css"),read("app/jobs/intelligence/page.tsx"),read("components/jobs/JobsNav.tsx")]);for(const path of["jobs/campaigns","jobs/employers","jobs/recruiters","/progress","/target","/events"])assert.match(client,new RegExp(path));for(const phrase of["Today’s next action","Weekly review","Identity is not verification","Consent-safe outreach brief","Nothing is sent automatically","Evidence timeline","Excluded"])assert.match(ui,new RegExp(phrase));assert.match(client,/automatic_send:false/);assert.match(css,/@media\(max-width:560px\)/);assert.match(page,/IntelligenceWorkspace/);assert.match(nav,/Intelligence/);console.log("LQ08 intelligence UI contract: PASS");