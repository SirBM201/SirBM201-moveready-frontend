"use client";

import { FormEvent, useEffect, useState } from "react";

import { ApiError, apiJson } from "@/lib/api";
import { profileSearchContract } from "@/lib/jobProfile";
import { JobCompany, JobAction, JobLead, JobRecruiter, JobsSummary, formatJobDate, formValue, jobLabel, optionalFormValue } from "@/lib/jobs";

const emptyCounts: JobsSummary["counts"] = { recommended_jobs: 0, target_companies: 0, recruiters: 0, applications: 0, resume_documents: 0, follow_ups_due: 0 };

function errorMessage(error: unknown, fallback: string) { const apiError = error as ApiError; if (apiError?.status === 401) return "Sign in to open your private Jobs workspace."; return apiError?.message || fallback; }
function actionDueLabel(action: JobAction) { const days = action.days_until_due; if (days === undefined) return formatJobDate(action.due_at); if (days < 0) return `Overdue by ${Math.abs(days)} day${Math.abs(days) === 1 ? "" : "s"}`; if (days === 0) return "Due today"; return `Due in ${days} day${days === 1 ? "" : "s"}`; }
function priorityExplanation(job: JobLead) { if (job.application_priority === "not_recommended") return "Do not prioritize"; if (job.application_priority === "verify_authorization") return "Verify work rights first"; if (job.application_priority === "out_of_scope") return "Outside search scope"; if (job.application_priority === "recommended") return "Recommended to review"; return "Consider after review"; }
function scopeExplanation(job: JobLead) { return job.search_scope_classification === "local" || job.search_scope_classification === "international" ? jobLabel(job.search_scope_classification) : job.search_scope_classification === "unknown" ? "Location not classified" : job.is_local_job ? "Local" : "International"; }

export default function JobsDashboard() {
  const [summary, setSummary] = useState<JobsSummary | null>(null); const [companies, setCompanies] = useState<JobCompany[]>([]); const [recruiters, setRecruiters] = useState<JobRecruiter[]>([]); const [loading, setLoading] = useState(true); const [saving, setSaving] = useState(false); const [signedOut, setSignedOut] = useState(false); const [loadFailed, setLoadFailed] = useState(false); const [message, setMessage] = useState("Loading your private job search...");
  async function load() { setLoading(true); setLoadFailed(false); try { const [summaryResponse, companyResponse, recruiterResponse] = await Promise.all([apiJson<JobsSummary & { ok: boolean }>("jobs/summary", { timeoutMs: 20000 }), apiJson<{ ok: boolean; companies: JobCompany[] }>("jobs/companies", { timeoutMs: 20000 }), apiJson<{ ok: boolean; recruiters: JobRecruiter[] }>("jobs/recruiters", { timeoutMs: 20000 })]); setSummary(summaryResponse); setCompanies(companyResponse.companies || []); setRecruiters(recruiterResponse.recruiters || []); setSignedOut(false); const contract = summaryResponse.search_contract || profileSearchContract(summaryResponse.profile); setMessage(contract.ready ? "Your intentional job-search workspace is ready." : summaryResponse.profile ? "Complete your search area before MoveReady prioritizes vacancies." : "Complete five short setup steps to activate job matching with your own experience, countries, and work rights."); } catch (error) { const apiError = error as ApiError; setSummary(null); setSignedOut(apiError?.status === 401); setLoadFailed(apiError?.status !== 401); setMessage(errorMessage(error, "Unable to load the Jobs workspace.")); } finally { setLoading(false); } }
  useEffect(() => { void load(); }, []);
  async function addJob(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const form = event.currentTarget; const data = new FormData(form); setSaving(true); setMessage("Saving and scoring this job lead..."); try { const response = await apiJson<{ ok: boolean; job: JobLead }>("jobs", { method: "POST", body: { job_title: formValue(data, "job_title"), company_id: optionalFormValue(data, "company_id"), recruiter_id: optionalFormValue(data, "recruiter_id"), country: formValue(data, "country"), province: optionalFormValue(data, "province"), city: optionalFormValue(data, "city"), job_url: optionalFormValue(data, "job_url"), source_name: optionalFormValue(data, "source_name"), skills: formValue(data, "skills"), description_summary: optionalFormValue(data, "description_summary"), visa_sponsorship_status: formValue(data, "visa_sponsorship_status") || "unknown", work_authorization_requirement: formValue(data, "work_authorization_requirement") || "unknown", relocation_support_status: formValue(data, "relocation_support_status") || "unknown", status: "open" }, timeoutMs: 20000 }); form.reset(); setMessage(`${response.job.job_title} saved. Match and application viability have been recalculated.`); await load(); } catch (error) { setMessage(errorMessage(error, "Unable to save the job lead.")); } finally { setSaving(false); } }
  async function saveApplication(job: JobLead) { if (!job.company_id || !job.company_name) { setMessage("Add the employer to this job lead before moving it into Applications."); return; } setSaving(true); try { const response = await apiJson<{ ok: boolean; created?: boolean; message?: string }>("jobs/applications", { method: "POST", body: { job_id: job.id, company_id: job.company_id, recruiter_id: job.recruiter_id, status: "saved" }, timeoutMs: 20000 }); setMessage(response.created === false ? response.message || `${job.job_title} is already in Applications.` : `${job.job_title} moved into Applications as Saved.`); await load(); } catch (error) { setMessage(errorMessage(error, "Unable to create the application record.")); } finally { setSaving(false); } }
  const counts = summary?.counts || emptyCounts; const profile = summary?.profile; const searchContract = summary?.search_contract || profileSearchContract(profile || null); const profileReady = Boolean(profile && searchContract.ready); const defaultVacancyCountry = searchContract.search_scope === "international" ? searchContract.international_target_countries[0] || "" : searchContract.current_country || "";
  const journeySteps = [{ title: "Set your search area and profile", detail: "Choose local, international, or both, then add your real experience, skills, countries, and work rights.", complete: profileReady, href: profileReady ? "/jobs/profile" : "/jobs/setup", action: profileReady ? "Review profile" : profile ? "Complete search area" : "Start setup" }, { title: "Choose target companies", detail: "Shortlist employers you genuinely want to research and approach.", complete: counts.target_companies > 0, href: "/jobs/companies", action: counts.target_companies > 0 ? "Review companies" : "Choose companies" }, { title: "Prepare your resume", detail: "Keep the resume or cover letter you will use for applications in one private place.", complete: counts.resume_documents > 0, href: "/jobs/resume-vault", action: counts.resume_documents > 0 ? "Open Resume Vault" : "Add a resume" }, { title: "Record a real vacancy", detail: "Save the original job link and compare career fit with application viability.", complete: counts.recommended_jobs > 0, href: "#add-job-lead", action: counts.recommended_jobs > 0 ? "Add another vacancy" : "Add a vacancy" }, { title: "Track your application", detail: "Record when you apply, what you sent, and when you should follow up.", complete: counts.applications > 0, href: "/jobs/applications", action: counts.applications > 0 ? "Review applications" : "Open applications" }];
  const completedJourneySteps = journeySteps.filter((item) => item.complete).length; const journeyProgress = Math.round((completedJourneySteps / journeySteps.length) * 100); const firstIncompleteStep = journeySteps.find((item) => !item.complete); const nextAction = counts.follow_ups_due > 0 && !firstIncompleteStep ? { title: "Review the actions that are due", detail: `${counts.follow_ups_due} recruiter or application follow-up${counts.follow_ups_due === 1 ? " is" : "s are"} waiting for you.`, href: "#jobs-action-center", action: "Review actions due" } : firstIncompleteStep || { title: "Keep your applications moving", detail: "Your job-search foundation is complete. Review applications and keep every follow-up date current.", href: "/jobs/applications", action: "Review applications" };
  if (loading && !summary && !signedOut) return <section className="section jobs-section"><article className="jobs-empty jobs-sign-in-card" aria-live="polite"><span className="eyebrow">Private Jobs workspace</span><h1>Opening your job-search plan...</h1><p>MoveReady is checking your profile, saved employers, resume files, applications, and follow-ups.</p></article></section>;
  if (signedOut) return <section className="section jobs-section"><article className="jobs-empty jobs-sign-in-card"><span className="eyebrow">Private Jobs workspace</span><h1>Sign in to continue your job search.</h1><p>Your profile, target employers, resume files, applications, and follow-ups are protected under your verified email account.</p><div className="actions"><a className="btn primary" href="/login?next=/jobs">Sign in with email</a><a className="btn" href="/">Return home</a></div></article></section>;
  if (loadFailed) return <section className="section jobs-section"><article className="jobs-empty jobs-sign-in-card"><span className="eyebrow">Jobs workspace unavailable</span><h1>We could not open your job-search plan.</h1><p>{message} Your saved information has not been changed.</p><div className="actions"><button className="btn primary" type="button" onClick={load} disabled={loading}>{loading ? "Trying again..." : "Try again"}</button><a className="btn" href="/dashboard">Open Dashboard</a></div></article></section>;
  return <>
    <section className="jobs-hero">
      <div><span className="eyebrow">Your job search, in one place</span><h1>Find jobs you can realistically pursue.</h1><p className="lede">MoveReady separates career fit from application viability, so local and international opportunities are judged by the signals that actually matter.</p><div className="actions"><a className="btn primary" href={nextAction.href}>{nextAction.action}</a><a className="btn" href="/jobs/automation">Automatic vacancy search</a><a className="btn" href="/jobs/applications">My applications</a><button className="btn" type="button" onClick={load} disabled={loading}>{loading ? "Refreshing..." : "Refresh"}</button></div><p className="form-status" aria-live="polite">{message}</p></div>
      <aside className="jobs-profile-card"><p className="overline">{profileReady ? "Active search contract" : "Search setup required"}</p><h2>{profile?.headline || "Create your personal job-search profile"}</h2>{profile ? <><div className="jobs-profile-facts"><span><strong>{profile.years_experience || 0} years</strong>Experience</span><span><strong>{jobLabel(searchContract.search_scope)}</strong>Search scope</span><span><strong>{searchContract.current_country || "Not recorded"}</strong>Current country</span></div><p>{searchContract.target_countries.length ? `Searching: ${searchContract.target_countries.join(", ")}` : "No country is ready for matching yet."}</p><p>Work rights reported: {searchContract.work_authorized_countries.join(", ") || "none yet — verification required"}.</p><div className="badge-row">{(profile.target_roles || []).slice(0, 4).map((role) => <span className="badge" key={role}>{role}</span>)}</div>{!profileReady ? <a className="text-link" href="/jobs/setup">Complete search setup</a> : <a className="text-link" href="/jobs/profile">Review search contract</a>}</> : <><p>Answer the guided questions to activate personal matching.</p><a className="text-link" href="/jobs/setup">Start guided setup</a></>}</aside>
    </section>
    <section className="section jobs-section jobs-journey-section"><div className="jobs-next-action"><div><p className="overline">Recommended next step</p><h2>{nextAction.title}</h2><p>{nextAction.detail}</p></div><a className="btn primary" href={nextAction.href}>{nextAction.action}</a></div><div className="jobs-journey-heading"><div><p className="overline">Your job-search journey</p><h2>One clear step at a time</h2></div><div className="jobs-progress-summary"><strong>{completedJourneySteps} of {journeySteps.length} complete</strong><div className="jobs-progress-track" role="progressbar" aria-valuemin={0} aria-valuemax={100} aria-valuenow={journeyProgress}><span style={{ width: `${journeyProgress}%` }} /></div></div></div><ol className="jobs-journey-steps">{journeySteps.map((item,index)=><li className={item.complete ? "complete" : firstIncompleteStep === item ? "current" : ""} key={item.title}><span className="jobs-step-number">{item.complete ? "✓" : index+1}</span><div><strong>{item.title}</strong><p>{item.detail}</p><a href={item.href}>{item.action}</a></div></li>)}</ol></section>
    <section className="jobs-metric-grid"><a href="/jobs" className="jobs-metric"><strong>{counts.recommended_jobs}</strong><span>Job leads</span></a><a href="/jobs/companies" className="jobs-metric"><strong>{counts.target_companies}</strong><span>Target companies</span></a><a href="/jobs/recruiters" className="jobs-metric"><strong>{counts.recruiters}</strong><span>Recruiters</span></a><a href="/jobs/applications" className="jobs-metric"><strong>{counts.applications}</strong><span>Applications</span></a><a href="/jobs/resume-vault" className="jobs-metric"><strong>{counts.resume_documents}</strong><span>Resume files</span></a><a href="#jobs-action-center" className={`jobs-metric ${counts.follow_ups_due ? "attention" : ""}`}><strong>{counts.follow_ups_due}</strong><span>Actions due</span></a></section>
    <section className="section jobs-section"><div className="section-heading-row"><div><p className="overline">Realistic opportunities</p><h2>Career match + application viability</h2><p className="section-intro">Match measures professional fit. Viability asks whether the vacancy is realistically actionable given location, work rights and employer support.</p></div><span className="status-dot">{summary?.recommended_jobs?.length || 0} active leads</span></div><div className="jobs-card-grid">{(summary?.recommended_jobs || []).map((job)=><article className="jobs-card" key={job.id}><div className="panel-heading"><div><p className="overline">{job.company_name || "Employer not linked"}</p><h3>{job.job_title}</h3></div><span className="jobs-score">{job.match_score || 0}% match</span></div><p>{[job.city,job.province,job.country].filter(Boolean).join(", ") || "Location not recorded"}</p><div className="badge-row"><span className="badge">{scopeExplanation(job)}</span><span className="badge">Viability {job.application_viability_score ?? job.application_priority_score ?? 0}%</span><span className="badge">{priorityExplanation(job)}</span></div><div className="badge-row"><span className="badge">Work rights: {jobLabel(job.work_authorization_requirement)}</span>{job.search_scope_classification !== "local" ? <span className="badge">Sponsor: {jobLabel(job.visa_sponsorship_status)}</span> : null}{job.search_scope_classification !== "local" && job.relocation_support_status ? <span className="badge">Relocation: {jobLabel(job.relocation_support_status)}</span> : null}</div><ul className="jobs-reasons">{(job.application_priority_reasons || job.viability_reasons || []).slice(0,2).map((reason)=><li key={reason}><strong>{reason}</strong></li>)}{(job.match_reasons || []).slice(0,2).map((reason)=><li key={reason}>{reason}</li>)}</ul><div className="actions">{job.job_url ? <a className="btn" href={job.job_url} target="_blank" rel="noreferrer">Verify vacancy</a> : null}<button className="btn primary" type="button" onClick={()=>saveApplication(job)} disabled={saving || !job.company_id || job.application_priority === "out_of_scope"}>{job.application_priority === "out_of_scope" ? "Outside selected scope" : job.application_priority === "not_recommended" ? "Save for review" : job.application_priority === "verify_authorization" ? "Save pending work-rights check" : "Save application"}</button></div></article>)}{!summary?.recommended_jobs?.length ? <article className="jobs-empty"><h3>No job leads recorded yet</h3><p>Add a real vacancy or activate automatic discovery. MoveReady will score career fit and application viability separately.</p></article> : null}</div></section>
    <section className="section jobs-section jobs-two-column" id="add-job-lead">{profileReady ? <form className="jobs-form" onSubmit={addJob}><div className="panel-heading"><div><p className="overline">Capture a vacancy</p><h2>Add a real job lead</h2></div><span className="status-dot">Private</span></div><p className="jobs-form-intro">Record what the vacancy actually says. Unknown evidence stays unknown.</p><div className="form-grid two-col"><div className="field"><label htmlFor="job_title">Job title</label><input id="job_title" name="job_title" required /></div><div className="field"><label htmlFor="job_company">Company</label><select id="job_company" name="company_id" defaultValue=""><option value="">Select a target company</option>{companies.map(c=><option value={c.id} key={c.id}>{c.company_name}</option>)}</select></div><div className="field"><label htmlFor="job_country">Country</label><input id="job_country" name="country" defaultValue={defaultVacancyCountry} required /><small>MoveReady will show if this country is outside your selected scope.</small></div><div className="field"><label htmlFor="job_url">Vacancy URL</label><input id="job_url" name="job_url" type="url" /></div></div><details className="jobs-form-more"><summary>Add matching and authorization details <span>Recommended for international jobs</span></summary><div className="jobs-form-more-fields"><div className="form-grid two-col"><div className="field"><label htmlFor="job_recruiter">Recruiter</label><select id="job_recruiter" name="recruiter_id" defaultValue=""><option value="">Not linked</option>{recruiters.map(r=><option value={r.id} key={r.id}>{r.recruiter_name}</option>)}</select></div><div className="field"><label htmlFor="job_province">Province/state/region</label><input id="job_province" name="province" /></div><div className="field"><label htmlFor="job_city">City</label><input id="job_city" name="city" /></div><div className="field"><label htmlFor="job_source">Source</label><input id="job_source" name="source_name" placeholder="Official career page" /></div><div className="field"><label htmlFor="job_authorization">Work authorization wording</label><select id="job_authorization" name="work_authorization_requirement" defaultValue="unknown"><option value="unknown">Not stated</option><option value="existing_required">Existing work authorization required</option><option value="employer_support_possible">Employer support may be possible</option><option value="employer_support_confirmed">Employer support confirmed</option></select></div><div className="field"><label htmlFor="job_sponsorship">Sponsorship wording</label><select id="job_sponsorship" name="visa_sponsorship_status" defaultValue="unknown"><option value="unknown">Not stated</option><option value="not_verified">Not verified</option><option value="possible">May be possible</option><option value="confirmed">Confirmed</option><option value="not_available">Not available</option></select></div><div className="field"><label htmlFor="job_relocation">Relocation support</label><select id="job_relocation" name="relocation_support_status" defaultValue="unknown"><option value="unknown">Not stated</option><option value="not_available">Not available</option><option value="possible">May be available</option><option value="confirmed">Confirmed</option></select></div><div className="field"><label htmlFor="job_skills">Important skills</label><input id="job_skills" name="skills" /><small>Separate skills with commas.</small></div></div><div className="field"><label htmlFor="job_summary">Short vacancy summary</label><textarea id="job_summary" name="description_summary" rows={4} /></div></div></details><button className="btn primary" type="submit" disabled={saving}>{saving ? "Saving..." : "Save and assess job"}</button></form> : <article className="jobs-empty jobs-profile-required"><h2>{profile ? "Complete your search area first." : "Create your job profile first."}</h2><p>MoveReady needs an intentional local, international, or combined country scope before it can prioritize vacancies safely.</p><a className="btn primary" href="/jobs/setup">{profile ? "Complete search setup" : "Set up my job search"}</a></article>}<aside className="jobs-pipeline-panel" id="jobs-action-center"><div className="panel-heading"><div><p className="overline">Application pipeline</p><h2>Status at a glance</h2></div><span className="status-dot">Live</span></div><div className="jobs-pipeline-list">{Object.entries(summary?.applications_by_status || {}).map(([status,count])=><a href={`/jobs/applications?status=${status}`} key={status}><span>{jobLabel(status)}</span><strong>{count}</strong></a>)}</div><div className="panel-heading"><h3>Priority work queue</h3><span className="status-dot">{summary?.action_counts?.overdue || 0} overdue</span></div>{(summary?.action_items || []).map(item=><a className="jobs-follow-up" href={item.href} key={`${item.kind}-${item.id}`}><strong>{jobLabel(item.priority)} · {item.title}</strong><span>{item.summary} · {actionDueLabel(item)}</span></a>)}{!summary?.action_items?.length ? <p>No recruiter or application follow-up is due in the next 14 days.</p> : null}<a className="text-link" href="/action-center">Open the full MoveReady Action Center</a></aside></section>
    <section className="jobs-safety-strip"><strong>Evidence boundary</strong><span>{summary?.privacy_note || "Sponsorship, work authorization, relocation support and vacancy status remain unverified until linked to a current source."}</span></section>
  </>;
}
