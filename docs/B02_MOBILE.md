# B02 mobile UX

Provenance guidance reuses the app's existing `section`, `result-block`, `mini-list`, `two-col-list`, `grid`, `card`, `badge` and `form-status` primitives rather than introducing fixed-width layouts. This keeps B02 aligned with the existing responsive CSS and avoids a separate mobile-only Passport implementation.
