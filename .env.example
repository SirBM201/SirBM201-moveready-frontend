NEXT_PUBLIC_APP_NAME=Project MoveReady
# Optional public browser proxy hint. URLs in NEXT_PUBLIC_* are visible to every visitor.
NEXT_PUBLIC_API_BASE=
# Public HTTPS backend origin only. Never place admin keys, service-role secrets,
# passwords, signing keys, or provider tokens in any NEXT_PUBLIC_* variable.
NEXT_PUBLIC_BACKEND_URL=https://moveready-mvp-production.up.railway.app
# Preferred server/build-time backend origin used by the Next.js rewrite contract.
MOVEREADY_BACKEND_URL=https://moveready-mvp-production.up.railway.app
